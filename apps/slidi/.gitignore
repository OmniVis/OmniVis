# Dependencies
/node_modules
/.pnp
.pnp.js

# Next.js Build Output
/.next/
/out/

# Production
/build

# Cloudflare / Wrangler
/.wrangler
/dist
.dev.vars

# Collab sidecar compiled output
/dist-collab
# Cloudflare D1 local database files
*.sqlite
*.sqlite-journal

# Environment Variables (Crucial for security)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debugging & Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE / System files
.DS_Store
*.pem
.vscode/
.idea/

# Slidi Specific Settings
# We keep CLAUDE.md and /plan because they are documentation,
# but we ignore local settings if you created a settings.local.json.
settings.local.json

# Optional: If you want to keep the memory folder on Git but
# ignore temporary system files inside it (rarely needed but safe)
# .claude/config.json

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Superpowers plugin runtime directory
.superpowers/

# Claude local session files (not for version control)
.claude/memory/
.claude/problems/
.claude/prompts/
.claude/checkpoints/
.claude/private/
.claude/worktrees/
certificates