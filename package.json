{
  "name": "omnivis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:graphi": "cd apps/graphi && pnpm install && pnpm run build",
    "build:slidi": "cd apps/slidi && npm install && npm run build",
    "build:launchpad": "echo 'Launchpad is static'",
    "build": "pnpm run build:graphi && pnpm run build:slidi"
  }
}
